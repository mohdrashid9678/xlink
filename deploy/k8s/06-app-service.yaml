apiVersion: v1
kind: Service
metadata:
  name: xlink-service
  namespace: xlink
  labels:
    app: xlink-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: xlink-api
